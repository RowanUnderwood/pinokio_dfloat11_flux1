{
  "run": [
    {
      "method": "notify",
      "params": {
        "type": "success",
        "html": "FLUX.1-dev example finished successfully! Image saved in the 'output' folder."
      }
    },
    {
      "method": "browser.open",
      "params": {
        "uri": "{{kernel.path(__dirname, 'output', 'flux_1_dev_output.png')}}"
      }
    }
  ]
}